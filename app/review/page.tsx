// M4: HITL review queue.
// Shows tickets that routing.ts flagged for human approval, with the AI's
// suggested category/priority/confidence, and approve/override controls.

export default function ReviewPage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Review Queue</h1>
      <p>Scaffold placeholder — built in M4.</p>
    </main>
  );
}
