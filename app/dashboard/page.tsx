// M5: metrics dashboard.
// Headline metric per the discovery brief: time-to-human-review for
// high-stakes categories (target under 15 minutes) — not generic ticket volume.

export default function DashboardPage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Dashboard</h1>
      <p>Scaffold placeholder — built in M5.</p>
    </main>
  );
}
